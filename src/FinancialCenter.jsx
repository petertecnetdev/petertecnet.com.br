import { useEffect, useMemo, useState } from 'react'
import './FinancialCenter.css'
const API='https://api.petertecnet.com.br/api'
async function api(path){const token=localStorage.getItem('token');const r=await fetch(`${API}${path}`,{headers:{Accept:'application/json',...(token?{Authorization:`Bearer ${token}`}:{})}});const d=await r.json().catch(()=>({}));if(r.status===401){localStorage.removeItem('token');window.location.href='/login'}if(!r.ok)throw new Error(d?.message||d?.error||'Falha ao consultar o financeiro.');return d}
const money=v=>Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}),pct=(a,b)=>b?`${((a/b)*100).toFixed(1)}%`:'0%',fmt=v=>v?new Date(v).toLocaleString('pt-BR'):'—'
const label=s=>({approved:'Aprovado',paid:'Pago',pending:'Pendente',in_process:'Processando',failed:'Falhou',rejected:'Rejeitado',refunded:'Estornado',charged_back:'Chargeback',processing:'Processando',completed:'Concluído'}[s]||s||'—')
export default function FinancialCenter(){const[d,setD]=useState(null),[tx,setTx]=useState(null),[po,setPo]=useState(null),[f,setF]=useState({app_slug:'',provider:'',status:'',from:'',to:''}),[selected,setSelected]=useState(null),[error,setError]=useState(''),[live,setLive]=useState(true);const qs=useMemo(()=>new URLSearchParams(Object.entries(f).filter(([,v])=>v)).toString(),[f]);async function load(silent=false){try{if(!silent)setError('');const[a,b,c]=await Promise.all([api(`/admin/ecosystem/financial/dashboard?${qs}`),api(`/admin/ecosystem/financial/transactions?${qs}&per_page=100`),api('/admin/ecosystem/financial/payouts')]);setD(a);setTx(b);setPo(c)}catch(e){setError(e.message)}}useEffect(()=>{load()},[qs]);useEffect(()=>{if(!live)return;const id=setInterval(()=>load(true),10000);return()=>clearInterval(id)},[live,qs]);async function inspect(id){try{setSelected((await api(`/admin/ecosystem/financial/transactions/${id}`)).transaction)}catch(e){setError(e.message)}}const s=d?.summary||{},total=Number(s.totals?.transactions||0),approved=Number(s.approved?.count||0),failed=Number(s.failed?.count||0);return <main className="finance-page"><div className="finance-topnav"><a href="/admin">← Admin Center</a><b>Peter Tecnet · Financeiro</b><button onClick={()=>load()}>Atualizar agora</button></div><div className="finance-stack"><section className="finance-hero"><div><p>FINANCIAL OPERATIONS CENTER</p><h2>Saúde financeira do ecossistema</h2><span>Vendas, pagamentos, taxas, liquidação, falhas, estornos e repasses em uma única visão operacional.</span></div><div className="finance-live"><i className={live?'on':''}/><b>{live?'Tempo real ativo':'Tempo real pausado'}</b><button onClick={()=>setLive(v=>!v)}>{live?'Pausar':'Ativar'}</button></div></section>{error&&<div className="finance-alert critical"><b>Falha no monitoramento</b><span>{error}</span></div>}<div className="finance-kpis"><Kpi title="Volume bruto" value={money(s.totals?.gross)} sub={`${total} transações`}/><Kpi title="Receita da plataforma" value={money(s.totals?.platform_fees)} sub="taxas Peter Tecnet"/><Kpi title="Líquido vendedores" value={money(s.totals?.seller_net)} sub="repasse dos vendedores"/><Kpi title="Aprovação" value={pct(approved,total)} sub={`${approved} aprovadas`}/><Kpi title="Falhas" value={pct(failed,total)} sub={`${failed} transações`} danger={failed>0}/><Kpi title="Estornos" value={money(s.refunded?.amount)} sub={`${s.refunded?.count||0} ocorrências`} danger={Number(s.refunded?.count)>0}/></div><section className="finance-panel"><header><div><b>Central de alertas</b><small>Anomalias e situações que exigem atenção</small></div></header><div className="finance-alerts">{d?.alerts?.length?d.alerts.map((a,i)=><div className={`finance-alert ${a.severity}`} key={i}><b>{a.title}</b><span>{a.message}</span></div>):<div className="finance-alert ok"><b>Operação saudável</b><span>Nenhum alerta financeiro detectado no período.</span></div>}</div></section><section className="finance-panel"><header><div><b>Filtros operacionais</b><small>Investigue aplicação, gateway, status e período</small></div><button onClick={()=>setF({app_slug:'',provider:'',status:'',from:'',to:''})}>Limpar</button></header><div className="finance-filters"><input placeholder="Aplicação (slug)" value={f.app_slug} onChange={e=>setF({...f,app_slug:e.target.value})}/><input placeholder="Provedor" value={f.provider} onChange={e=>setF({...f,provider:e.target.value})}/><select value={f.status} onChange={e=>setF({...f,status:e.target.value})}><option value="">Todos os status</option><option value="approved">Aprovado</option><option value="pending">Pendente</option><option value="failed">Falhou</option><option value="rejected">Rejeitado</option><option value="refunded">Estornado</option></select><input type="date" value={f.from} onChange={e=>setF({...f,from:e.target.value})}/><input type="date" value={f.to} onChange={e=>setF({...f,to:e.target.value})}/></div></section><div className="finance-grid"><Panel title="Performance por aplicação"><Table heads={['Aplicação','Tx','Bruto','Taxas','Líquido']} rows={d?.applications?.map(x=>[x.application_name||x.app_slug,x.transactions,money(x.gross),money(x.platform_fees),money(x.seller_net)])}/></Panel><Panel title="Gateways / provedores"><Table heads={['Provedor','Tx','Aprov.','Falhas','Sucesso']} rows={d?.providers?.map(x=>[x.provider,x.transactions,x.approved,x.failed,pct(x.approved,x.transactions)])}/></Panel></div><Panel title="Fluxo dos últimos 30 dias"><div className="finance-chart">{d?.timeline?.map(x=>{const max=Math.max(...d.timeline.map(i=>Number(i.gross||0)),1);return <div className="finance-bar" key={x.day} title={`${x.day}: ${money(x.gross)}`}><span style={{height:`${Math.max(4,(Number(x.gross)/max)*100)}%`}}/><small>{String(x.day).slice(5)}</small></div>})}</div></Panel><Panel title={`Transações · ${tx?.total||0}`}><div className="finance-table-wrap"><table><thead><tr><th>Data</th><th>Aplicação</th><th>Provedor</th><th>Referência</th><th>Método</th><th>Status</th><th>Bruto</th><th>Taxa</th><th>Líquido</th></tr></thead><tbody>{tx?.data?.map(t=><tr key={t.id} onClick={()=>inspect(t.id)}><td>{fmt(t.created_at)}</td><td>{t.application_name||t.app_slug}</td><td>{t.provider}</td><td>{t.source_reference}</td><td>{t.method||'—'}</td><td><span className={`finance-status ${t.status}`}>{label(t.status)}</span></td><td>{money(t.gross_amount)}</td><td>{money(t.platform_fee)}</td><td>{money(t.seller_net)}</td></tr>)}</tbody></table></div></Panel><Panel title={`Repasses e saques · ${po?.summary?.pending||0} pendentes`}><Table heads={['Criado','Status','Valor','Produção','Referência']} rows={po?.data?.map(x=>[fmt(x.created_at),label(x.status),money(x.amount||x.net_amount),x.production_id||'—',x.public_id||x.id])}/></Panel>{selected&&<div className="finance-modal" onClick={()=>setSelected(null)}><article onClick={e=>e.stopPropagation()}><header><div><small>TRANSAÇÃO #{selected.id}</small><h3>{money(selected.gross_amount)}</h3></div><button onClick={()=>setSelected(null)}>×</button></header><dl>{Object.entries(selected).filter(([k])=>k!=='metadata').map(([k,v])=><div key={k}><dt>{k}</dt><dd>{String(v??'—')}</dd></div>)}</dl><h4>Metadata</h4><pre>{typeof selected.metadata==='string'?selected.metadata:JSON.stringify(selected.metadata,null,2)}</pre></article></div>}</div></main>}
function Kpi({title,value,sub,danger}){return <article className={`finance-kpi ${danger?'danger':''}`}><span>{title}</span><strong>{value}</strong><small>{sub}</small></article>}
function Panel({title,children}){return <section className="finance-panel"><header><div><b>{title}</b></div></header>{children}</section>}
function Table({heads,rows=[]}){return <div className="finance-table-wrap"><table><thead><tr>{heads.map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{rows?.length?rows.map((r,i)=><tr key={i}>{r.map((c,j)=><td key={j}>{c}</td>)}</tr>):<tr><td colSpan={heads.length}>Sem dados no período.</td></tr>}</tbody></table></div>}
