import { useEffect, useMemo, useState } from 'react'
import './Admin.css'
import './Login.css'
import CommandCenter from './CommandCenter.jsx'
import FinancialCenter from './FinancialCenter.jsx'

const API = 'https://api.petertecnet.com.br/api'
const TOKEN_KEY = 'token'
const TABS = [
  ['command', 'Mission Control'], ['dashboard', 'Visão geral'], ['activity', 'Atividade'], ['financial', 'Financeiro'], ['applications', 'Aplicações'], ['users', 'Usuários'],
  ['profiles', 'Perfis e permissões'], ['establishments', 'Estabelecimentos'], ['items', 'Itens'], ['site', 'Site institucional'], ['audit', 'Auditoria'],
]
const emptyApp = { name: '', description: '', slug: '', url: '', logo: '', version: '', author: 'Peter Tecnet', release_date: '', is_active: true }
const emptyUser = { first_name: '', last_name: '', user_name: '', email: '', password: '', profile_id: '' }
const tokenFrom = data => data?.token?.access_token || data?.token?.original?.access_token || data?.access_token || data?.token
const fmt = value => value ? new Date(value).toLocaleString('pt-BR') : '—'
const fullName = user => [user?.first_name, user?.last_name].filter(Boolean).join(' ') || user?.user_name || 'Usuário'
const tabFromLocation = () => window.location.pathname.replace(/\/+$/, '') === '/admin/financial' ? 'financial' : 'command'

function ProcessingIndicator({ message = 'Carregando...' }) { return <div className="peter-processing" role="status" aria-live="polite"><div className="peter-processing__visual"><i /><i /><img src="/petertecnetlogo.png" alt="" /></div><strong>{message}</strong></div> }
async function request(path, options = {}) { const token=localStorage.getItem(TOKEN_KEY); const controller=new AbortController(); const timeout=window.setTimeout(()=>controller.abort(),20000); try { const response=await fetch(`${API}${path}`,{...options,signal:controller.signal,headers:{Accept:'application/json','Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...options.headers}}); const data=response.status===204?null:await response.json().catch(()=>({})); if(response.status===401&&path!=='/auth/login'){localStorage.removeItem(TOKEN_KEY);window.location.href='/login'} if(!response.ok)throw new Error(data?.error||data?.message||Object.values(data?.errors||{})?.flat()?.[0]||'Não foi possível concluir a operação.'); return data } catch(error){if(error.name==='AbortError')throw new Error('A API demorou para responder. Tente novamente.');throw error} finally{window.clearTimeout(timeout)} }

export function LoginPage(){const[form,setForm]=useState({username:'',password:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(event){event.preventDefault();setLoading(true);setError('');try{const data=await request('/auth/login',{method:'POST',body:JSON.stringify({username:form.username.trim(),password:form.password})});const token=tokenFrom(data);if(!token)throw new Error('A API não retornou um token de acesso.');localStorage.setItem(TOKEN_KEY,token);window.location.href='/admin'}catch(err){setError(err.message)}finally{setLoading(false)}}return <>{loading&&<ProcessingIndicator message="Autenticando acesso..."/>}<main className="login-page"><div className="login-effects"><i/><i/><span/></div><div className="login-layout"><aside className="login-hero"><div><a className="admin-brand" href="/"><img src="/petertecnetlogo.png" alt=""/><span>Peter Tecnet</span></a><p className="admin-kicker">Central de governança</p><h1>Controle todo o<br/><em>ecossistema digital.</em></h1><p>Aplicações, usuários, acessos, comportamento, segurança, financeiro e auditoria em um único painel.</p></div><small>🔐 Acesso administrativo protegido pela API Peter Tecnet.</small></aside><section className="login-panel"><form className="login-card" onSubmit={submit}><div className="login-logo"><img src="/petertecnetlogo.png" alt="Peter Tecnet"/></div><h2>Acesso administrativo</h2><p>Entre para administrar o ecossistema Peter Tecnet.</p><label>Usuário ou e-mail<div className="login-input"><span>✉</span><input autoFocus autoComplete="username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})} required/></div></label><label>Senha<div className="login-input"><span>🔒</span><input type="password" autoComplete="current-password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/></div></label>{error&&<p className="admin-error" role="alert">{error}</p>}<button className="admin-primary" disabled={loading}>Entrar</button><a className="admin-back" href="/">← Voltar para o site</a></form></section></div></main></>}

export function AdminPage(){const[tab,setTab]=useState(tabFromLocation);const[data,setData]=useState({});const[processing,setProcessing]=useState(false);const[error,setError]=useState('');const[message,setMessage]=useState('');const[userDetail,setUserDetail]=useState(null);const paths={dashboard:'/admin/ecosystem/dashboard',activity:'/admin/ecosystem/activity',applications:'/admin/applications',users:'/admin/ecosystem/users',profiles:'/admin/ecosystem/profiles',establishments:'/admin/ecosystem/establishments',items:'/admin/ecosystem/items',site:'/admin/ecosystem/settings',audit:'/admin/ecosystem/audit'};async function load(target=tab,customPath){if(['financial','command'].includes(target)){setProcessing(false);return null}setProcessing(true);setError('');try{const result=await request(customPath||paths[target]);setData(prev=>({...prev,[target]:result}));return result}catch(err){setError(err.message)}finally{setProcessing(false)}}async function openUser(user){setProcessing(true);setError('');try{setUserDetail(await request(`/admin/ecosystem/users/${user.id}`))}catch(err){setError(err.message)}finally{setProcessing(false)}}useEffect(()=>{if(!localStorage.getItem(TOKEN_KEY)){window.location.href='/login';return}if(!['financial','command'].includes(tab))load(tab)},[]);useEffect(()=>{if(localStorage.getItem(TOKEN_KEY)&&!['financial','command'].includes(tab)&&!data[tab])load(tab)},[tab]);useEffect(()=>{const onPop=()=>setTab(tabFromLocation());window.addEventListener('popstate',onPop);return()=>window.removeEventListener('popstate',onPop)},[]);async function mutate(path,options,success,reload=tab){setProcessing(true);setError('');setMessage('');try{await request(path,options);setMessage(success);await load(reload);if(reload!=='dashboard')setData(prev=>({...prev,dashboard:undefined}));if(userDetail?.user?.id)setUserDetail(await request(`/admin/ecosystem/users/${userDetail.user.id}`))}catch(err){setError(err.message);setProcessing(false)}}function changeTab(key){setTab(key);setUserDetail(null);setMessage('');setError('');const next=key==='financial'?'/admin/financial':'/admin';if(window.location.pathname!==next)window.history.pushState({},'',next)}function logout(){localStorage.removeItem(TOKEN_KEY);window.location.href='/login'}const counts=data.dashboard?.summary||{};return <>{processing&&<ProcessingIndicator message="Atualizando ecossistema..."/>}<main className="ecosystem-shell"><aside className="ecosystem-sidebar"><a className="admin-brand ecosystem-brand" href="/"><img src="/petertecnetlogo.png" alt=""/><span><b>Peter Tecnet</b><small>Mission Control</small></span></a><nav>{TABS.map(([key,label])=><button key={key} className={tab===key?'active':''} onClick={()=>changeTab(key)}>{label}</button>)}</nav><div className="sidebar-foot"><a href="/" target="_blank">Abrir site ↗</a><button onClick={logout}>Sair</button></div></aside><section className="ecosystem-main"><header className="ecosystem-top"><div><p className="admin-kicker">Peter Tecnet Command Center</p><h1>{userDetail?'Detalhes do usuário':TABS.find(x=>x[0]===tab)?.[1]}</h1></div><div className="top-actions">{userDetail&&<button onClick={()=>setUserDetail(null)}>← Voltar</button>}{!['financial','command'].includes(tab)&&<button onClick={()=>userDetail?openUser(userDetail.user):load(tab)}>Atualizar</button>}</div></header>{error&&<div className="notice error">{error}</div>}{message&&<div className="notice success">{message}</div>}{userDetail?<UserDetail payload={userDetail} mutate={mutate}/>:<>{tab==='command'&&<CommandCenter/>} {tab==='dashboard'&&<Dashboard summary={counts} payload={data.dashboard} onUser={openUser}/>} {tab==='activity'&&<Activity payload={data.activity} applications={data.applications} users={data.users} load={load} setData={setData}/>} {tab==='financial'&&<FinancialCenter embedded/>} {tab==='applications'&&<Applications payload={data.applications} mutate={mutate}/>} {tab==='users'&&<Users payload={data.users} profiles={data.profiles} applications={data.applications} load={load} mutate={mutate} openUser={openUser}/>} {tab==='profiles'&&<Profiles payload={data.profiles} mutate={mutate}/>} {tab==='establishments'&&<Establishments payload={data.establishments} applications={data.applications} load={load} mutate={mutate}/>} {tab==='items'&&<Items payload={data.items}/>} {tab==='site'&&<SiteSettings payload={data.site} mutate={mutate}/>} {tab==='audit'&&<Audit payload={data.audit}/>}</>}</section></main></>}

function Dashboard({summary,payload,onUser}){const cards=[['Usuários ativos hoje',summary.active_users_today,`${summary.interactions_today||0} interações`],['Ativos em 7 dias',summary.active_users_7d,'usuários únicos'],['Ativos em 30 dias',summary.active_users_30d,`${summary.inactive_users_30d||0} inativos`],['Novos usuários',summary.new_users_30d,'últimos 30 dias'],['Aplicações',summary.applications,`${summary.active_applications||0} ativas`],['Usuários',summary.users,`${summary.access_links||0} vínculos`],['Estabelecimentos',summary.establishments,`${summary.approved_establishments||0} aprovados`],['Interações 30 dias',summary.interactions_30d,'atividade registrada']];return <div className="admin-stack"><div className="metric-grid">{cards.map(([label,value,sub])=><article className="metric-card" key={label}><span>{label}</span><strong>{value??'—'}</strong><small>{sub}</small></article>)}</div><div className="admin-grid"><Panel title="Uso das aplicações"><div className="table-wrap"><table><thead><tr><th>Aplicação</th><th>Usuários</th><th>Ativos 30d</th><th>Interações 30d</th></tr></thead><tbody>{payload?.applications?.map(app=><tr key={app.id}><td><b>{app.name}</b></td><td>{app.users_count}</td><td>{app.active_users_30d||0}</td><td>{app.activity_count_30d||0}</td></tr>)}</tbody></table></div></Panel><Panel title="Tipos de atividade">{payload?.activity_types?.length?payload.activity_types.map(item=><div className="stat-row" key={item.interaction_type}><span>{humanType(item.interaction_type)}</span><b>{item.total}</b></div>):<Empty/>}</Panel></div><div className="admin-grid"><Panel title="Atividade recente"><Timeline rows={payload?.recent_activity||[]} compact/></Panel><Panel title="Usuários recentes">{payload?.recent_users?.map(user=><button className="person-row" key={user.id} onClick={()=>onUser(user)}><span><b>{fullName(user)}</b><small>{user.email}</small></span><i>{fmt(user.created_at)}</i></button>)}</Panel></div></div>}
function Activity({payload,applications,users,load,setData}){const[filters,setFilters]=useState({user_id:'',app_id:'',type:'',from:'',to:'',search:''});useEffect(()=>{if(!applications)load('applications');if(!users)load('users')},[]);async function apply(e){e?.preventDefault();const qs=new URLSearchParams(Object.entries(filters).filter(([,v])=>v));const result=await load('activity',`/admin/ecosystem/activity?${qs}`);if(result)setData(prev=>({...prev,activity:result}))}return <div className="admin-stack"><div className="metric-grid three"><Metric label="Interações" value={payload?.summary?.total} sub="no filtro atual"/><Metric label="Usuários" value={payload?.summary?.users} sub="usuários únicos"/><Metric label="Aplicações" value={payload?.summary?.applications} sub="com atividade"/></div><Panel title="Filtros de atividade"><form className="filter-grid" onSubmit={apply}><Field label="Busca" value={filters.search} onChange={v=>setFilters({...filters,search:v})}/><label>Aplicação<select value={filters.app_id} onChange={e=>setFilters({...filters,app_id:e.target.value})}><option value="">Todas</option>{applications?.applications?.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label>Usuário<select value={filters.user_id} onChange={e=>setFilters({...filters,user_id:e.target.value})}><option value="">Todos</option>{users?.users?.map(u=><option key={u.id} value={u.id}>{fullName(u)} · {u.email}</option>)}</select></label><label>Ação<select value={filters.type} onChange={e=>setFilters({...filters,type:e.target.value})}><option value="">Todas</option>{payload?.types?.map(t=><option key={t} value={t}>{humanType(t)}</option>)}</select></label><Field label="De" type="date" value={filters.from} onChange={v=>setFilters({...filters,from:v})}/><Field label="Até" type="date" value={filters.to} onChange={v=>setFilters({...filters,to:v})}/><button className="primary">Aplicar filtros</button></form></Panel><Panel title="Linha do tempo do ecossistema"><Timeline rows={payload?.activity||[]}/></Panel></div>}
function Users({payload,profiles,applications,load,mutate,openUser}){const[form,setForm]=useState(emptyUser);const[editing,setEditing]=useState(null);const[search,setSearch]=useState('');const users=payload?.users||[];useEffect(()=>{if(!profiles)load('profiles');if(!applications)load('applications')},[]);async function doSearch(e){e.preventDefault();await load('users',`/admin/ecosystem/users${search?`?search=${encodeURIComponent(search)}`:''}`)}function submit(e){e.preventDefault();const body={...form,profile_id:form.profile_id||null};if(editing)delete body.password;mutate(`/admin/ecosystem/users${editing?`/${editing}`:''}`,{method:editing?'PUT':'POST',body:JSON.stringify(body)},editing?'Usuário atualizado.':'Usuário criado.','users');setEditing(null);setForm(emptyUser)}function edit(user){setEditing(user.id);setForm({first_name:user.first_name||'',last_name:user.last_name||'',user_name:user.user_name||'',email:user.email||'',password:'',profile_id:user.profile_id||''})}function access(user,app){const current=user.applications?.find(a=>a.id===app.id);const status=current?.pivot?.status==='active'?'blocked':'active';mutate(`/admin/ecosystem/users/${user.id}/applications/${app.id}`,{method:'PUT',body:JSON.stringify({status,role:current?.pivot?.role||'member'})},`${app.name}: acesso ${status==='active'?'liberado':'bloqueado'}.`,'users')}return <div className="admin-stack"><div className="admin-grid"><Panel title={editing?'Editar usuário':'Novo usuário'}><form onSubmit={submit} className="form-grid"><Field label="Nome" value={form.first_name} onChange={v=>setForm({...form,first_name:v})} required/><Field label="Sobrenome" value={form.last_name} onChange={v=>setForm({...form,last_name:v})}/><Field label="Usuário" value={form.user_name} onChange={v=>setForm({...form,user_name:v})} required/><Field label="E-mail" value={form.email} onChange={v=>setForm({...form,email:v})} required/><label className="wide">Perfil<select value={form.profile_id} onChange={e=>setForm({...form,profile_id:e.target.value})}><option value="">Sem perfil</option>{profiles?.profiles?.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label>{!editing&&<Field label="Senha inicial" type="password" value={form.password} onChange={v=>setForm({...form,password:v})} wide required/>}<div className="form-actions wide"><button className="primary">Salvar usuário</button>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(emptyUser)}}>Cancelar</button>}</div></form></Panel><Panel title="Pesquisar usuários"><form className="search-form" onSubmit={doSearch}><input placeholder="Nome, e-mail ou usuário" value={search} onChange={e=>setSearch(e.target.value)}/><button className="primary">Buscar</button></form><p className="helper">Clique em “Detalhes” para abrir atividade, segurança, acessos e recursos do usuário.</p></Panel></div><Panel title={`Usuários (${users.length})`}><div className="user-list">{users.map(user=><article className="user-card" key={user.id}><div className="user-head"><div><b>{fullName(user)}</b><span>{user.email} · {user.profile?.name||'Sem perfil'}</span><small>Última atividade: {fmt(user.last_activity_at)} · {user.interactions_count||0} interações · {user.establishments_count||0} estabelecimentos</small></div><div><button className="primary" onClick={()=>openUser(user)}>Detalhes</button><button onClick={()=>edit(user)}>Editar</button><button className="danger" onClick={()=>window.confirm(`Excluir ${user.email}?`)&&mutate(`/admin/ecosystem/users/${user.id}`,{method:'DELETE'},'Usuário excluído.','users')}>Excluir</button></div></div><div className="access-grid">{applications?.applications?.map(app=>{const link=user.applications?.find(a=>a.id===app.id);const active=link?.pivot?.status==='active';return <button key={app.id} className={active?'access active':'access'} onClick={()=>access(user,app)}><b>{app.name}</b><small>{link?(active?'Permitido':link.pivot.status):'Sem vínculo'}</small></button>})}</div></article>)}</div></Panel></div>}
function UserDetail({payload,mutate}){const user=payload?.user||{};const summary=payload?.summary||{};const security=payload?.security||{};const resources=payload?.resources||{};return <div className="admin-stack"><section className="user-hero"><div className="avatar-large">{user.avatar?<img src={user.avatar} alt=""/>:<span>{(user.first_name?.[0]||user.user_name?.[0]||'U').toUpperCase()}</span>}</div><div><p className="admin-kicker">Usuário #{user.id}</p><h2>{fullName(user)}</h2><p>{user.email} · @{user.user_name||'—'} · {user.profile?.name||'Sem perfil'}</p><small>Criado em {fmt(user.created_at)} · {user.city||'Cidade não informada'}{user.uf?`/${user.uf}`:''}</small></div></section><div className="metric-grid"><Metric label="Interações" value={summary.total_interactions} sub={`${summary.interactions_30d||0} em 30 dias`}/><Metric label="Logins 30 dias" value={summary.logins_30d} sub={`${summary.logins_7d||0} em 7 dias`}/><Metric label="Aplicações" value={summary.applications} sub="vínculos existentes"/><Metric label="Estabelecimentos" value={summary.establishments} sub="recursos vinculados"/></div><div className="admin-grid"><Panel title="Resumo de atividade"><Info label="Última atividade" value={fmt(summary.last_activity_at)}/><Info label="Último login" value={fmt(summary.last_login_at)}/><Info label="Primeira atividade" value={fmt(summary.first_activity_at)}/><Info label="Logins 90 dias" value={summary.logins_90d??0}/>{payload?.activity_by_type?.map(item=><div className="stat-row" key={item.interaction_type}><span>{humanType(item.interaction_type)}</span><b>{item.total}</b></div>)}</Panel><Panel title="Aplicações e acessos"><div className="detail-apps">{user.applications?.map(app=><div className="detail-app" key={app.id}><div><b>{app.name}</b><small>{app.pivot?.role||'member'} · {app.pivot?.status||'sem status'}</small></div><button className={app.pivot?.status==='active'?'danger':'primary'} onClick={()=>mutate(`/admin/ecosystem/users/${user.id}/applications/${app.id}`,{method:'PUT',body:JSON.stringify({status:app.pivot?.status==='active'?'blocked':'active',role:app.pivot?.role||'member'})},'Acesso atualizado.','users')}>{app.pivot?.status==='active'?'Bloquear':'Liberar'}</button></div>)}{!user.applications?.length&&<Empty/>}</div></Panel></div><div className="admin-grid"><Panel title="Segurança e acesso">{security.alerts?.map((a,i)=><div className={`security-alert ${a.level}`} key={i}>{a.message}</div>)}</Panel><Panel title="Recursos do usuário"><Info label="Estabelecimentos" value={resources.establishments?.length||0}/></Panel></div><Panel title="Linha do tempo do usuário"><Timeline rows={payload?.timeline||[]}/></Panel></div>}
function Applications({payload,mutate}){const[form,setForm]=useState(emptyApp);const[editing,setEditing]=useState(null);const apps=payload?.applications||[];function edit(app){setEditing(app.id);setForm({...emptyApp,...app,release_date:app.release_date?.slice(0,10)||''})}function submit(e){e.preventDefault();mutate(`/admin/applications${editing?`/${editing}`:''}`,{method:editing?'PUT':'POST',body:JSON.stringify(form)},editing?'Aplicação atualizada.':'Aplicação cadastrada.','applications');setEditing(null);setForm(emptyApp)}return <div className="admin-grid"><Panel title={editing?'Editar aplicação':'Nova aplicação'}><form onSubmit={submit} className="form-grid"><Field label="Nome" value={form.name} onChange={v=>setForm({...form,name:v})} required/><Field label="Slug" value={form.slug} onChange={v=>setForm({...form,slug:v})}/><Field label="URL" value={form.url} onChange={v=>setForm({...form,url:v})} wide required/><Field label="Logo" value={form.logo} onChange={v=>setForm({...form,logo:v})} wide/><Field label="Versão" value={form.version} onChange={v=>setForm({...form,version:v})}/><Field label="Autor" value={form.author} onChange={v=>setForm({...form,author:v})}/><label className="wide">Descrição<textarea rows="4" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label className="check wide"><input type="checkbox" checked={!!form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> Aplicação ativa e visível</label><div className="form-actions wide"><button className="primary">Salvar</button>{editing&&<button type="button" onClick={()=>{setEditing(null);setForm(emptyApp)}}>Cancelar</button>}</div></form></Panel><Panel title="Aplicações cadastradas">{apps.map(app=><div className="entity-row" key={app.id}><img src={app.logo||'/petertecnetlogo.png'} alt=""/><div><b>{app.name}</b><span>{app.url}</span></div><i className={app.is_active?'status on':'status'}>{app.is_active?'Ativa':'Oculta'}</i><button onClick={()=>edit(app)}>Editar</button><button className="danger" onClick={()=>window.confirm(`Excluir ${app.name}?`)&&mutate(`/admin/applications/${app.id}`,{method:'DELETE'},'Aplicação excluída.','applications')}>Excluir</button></div>)}</Panel></div>}
function Profiles({payload,mutate}){const[selected,setSelected]=useState(null);const[name,setName]=useState('');const[permissions,setPermissions]=useState([]);const profiles=payload?.profiles||[];const available=payload?.permissions||[];function choose(p){setSelected(p.id);setName(p.name);setPermissions(p.permissions||[])}function create(){setSelected('new');setName('');setPermissions([])}function save(e){e.preventDefault();mutate(`/admin/ecosystem/profiles${selected&&selected!=='new'?`/${selected}`:''}`,{method:selected&&selected!=='new'?'PUT':'POST',body:JSON.stringify({name,permissions})},'Perfil salvo.','profiles')}return <div className="admin-grid"><Panel title="Perfis"><button className="primary compact" onClick={create}>+ Novo perfil</button>{profiles.map(p=><button className={`profile-row ${selected===p.id?'active':''}`} key={p.id} onClick={()=>choose(p)}><span><b>{p.name}</b><small>{p.users_count} usuários</small></span><i>{p.permissions?.length||0} permissões</i></button>)}</Panel><Panel title="Permissões do perfil">{selected?<form onSubmit={save}><Field label="Nome do perfil" value={name} onChange={setName} required/><div className="permission-list">{available.map(p=><label key={p.key}><input type="checkbox" checked={permissions.includes(p.key)} onChange={e=>setPermissions(e.target.checked?[...permissions,p.key]:permissions.filter(x=>x!==p.key))}/><span><b>{p.name}</b><small>{p.category} · {p.description}</small></span></label>)}</div><button className="primary">Salvar perfil e permissões</button></form>:<Empty text="Selecione ou crie um perfil."/>}</Panel></div>}
function Establishments({payload,applications,load,mutate}){const rows=payload?.establishments||[];useEffect(()=>{if(!applications)load('applications')},[]);function patch(item,changes){mutate(`/admin/ecosystem/establishments/${item.id}`,{method:'PUT',body:JSON.stringify(changes)},'Estabelecimento atualizado.','establishments')}return <Panel title={`Estabelecimentos (${rows.length})`}><div className="table-wrap"><table><thead><tr><th>Estabelecimento</th><th>Aplicação</th><th>Responsável</th><th>Status</th><th>Ações</th></tr></thead><tbody>{rows.map(item=><tr key={item.id}><td><b>{item.fantasy||item.name}</b></td><td>{item.application?.name||'—'}</td><td>{item.user?.email||'—'}</td><td><span className={item.is_approved?'status on':'status'}>{item.is_approved?'Aprovado':'Pendente'}</span></td><td className="row-actions"><button onClick={()=>patch(item,{is_approved:!item.is_approved})}>{item.is_approved?'Retirar aprovação':'Aprovar'}</button></td></tr>)}</tbody></table></div></Panel>}
function Items({payload}){const rows=payload?.items||payload?.data||[];return <Panel title={`Itens (${rows.length})`}><div className="table-wrap"><table><thead><tr><th>ID</th><th>Item</th><th>Aplicação</th><th>Estabelecimento</th><th>Preço</th><th>Status</th></tr></thead><tbody>{rows.length?rows.map(item=><tr key={item.id}><td>#{item.id}</td><td><b>{item.name}</b><small>{item.slug||''}</small></td><td>{item.application?.name||item.app?.name||item.app_id||'—'}</td><td>{item.establishment?.fantasy||item.establishment?.name||item.establishment_id||'—'}</td><td>{item.price!=null?Number(item.price).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'—'}</td><td><span className={item.is_active===false?'status':'status on'}>{item.is_active===false?'Inativo':'Ativo'}</span></td></tr>):<tr><td colSpan="6">Nenhum item encontrado.</td></tr>}</tbody></table></div></Panel>}
function SiteSettings({payload,mutate}){const[site,setSite]=useState(payload?.site||{});useEffect(()=>{if(payload?.site)setSite(payload.site)},[payload]);const contact=site.contact||{};function save(e){e.preventDefault();mutate('/admin/ecosystem/settings',{method:'PUT',body:JSON.stringify({site})},'Site institucional atualizado.','site')}return <form onSubmit={save} className="admin-stack"><Panel title="Contato"><div className="form-grid"><Field label="E-mail" value={contact.email||''} onChange={v=>setSite({...site,contact:{...contact,email:v}})}/><Field label="Instagram" value={contact.instagram||''} onChange={v=>setSite({...site,contact:{...contact,instagram:v}})}/></div></Panel><button className="primary save-site">Publicar alterações do site</button></form>}
function Audit({payload}){const logs=payload?.logs||[];return <Panel title={`Histórico administrativo (${logs.length})`}><div className="table-wrap"><table><thead><tr><th>Data</th><th>Administrador</th><th>Ação</th><th>Entidade</th></tr></thead><tbody>{logs.map(log=><tr key={log.id}><td>{fmt(log.created_at)}</td><td>{log.user?.email||'Sistema'}</td><td><b>{log.action}</b></td><td>{log.entity_type?.split('\\').pop()||'—'} {log.entity_id||''}</td></tr>)}</tbody></table></div></Panel>}
function Timeline({rows,compact=false}){if(!rows?.length)return <Empty/>;return <div className={`timeline ${compact?'compact':''}`}>{rows.map(row=><article className="timeline-row" key={row.id}><span className="timeline-dot"/><div><div className="timeline-title"><b>{humanType(row.type)}</b><i>{row.application?.name||'Aplicação não identificada'}</i></div><p>{row.user?`${fullName(row.user)} · ${row.user.email}`:'Visitante / sistema'}{row.name?` · ${row.name}`:''}</p><small>{fmt(row.created_at)}</small></div></article>)}</div>}
function humanType(type=''){const map={login:'Login',login_google:'Login Google',logout:'Logout',register:'Cadastro',view:'Visualização',update:'Atualização'};return map[type]||type.replaceAll('_',' ').replace(/\b\w/g,x=>x.toUpperCase())}
function Metric({label,value,sub}){return <article className="metric-card"><span>{label}</span><strong>{value??'—'}</strong><small>{sub}</small></article>}
function Info({label,value}){return <div className="info-row"><span>{label}</span><b>{value??'—'}</b></div>}
function Panel({title,children}){return <section className="admin-panel"><div className="panel-title"><h2>{title}</h2></div>{children}</section>}
function Field({label,value,onChange,wide,type='text',required}){return <label className={wide?'wide':''}>{label}<input type={type} value={value??''} onChange={e=>onChange(e.target.value)} required={required}/></label>}
function Empty({text='Nenhum registro encontrado.'}){return <p className="admin-empty">{text}</p>}
